/**
 * Configuración del lanzador de Padel Zone.
 *
 * Este es el ÚNICO archivo que se edita al instalar.
 * Cuando actualices index.html, este queda intacto.
 */
window.PZ_CONFIG = {
  // La URL de la aplicación web, la que termina en /exec
  app: "PEGA_AQUI_TU_URL"
};
